import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  backButton: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  backArrow: {
    marginRight: 5,
  },
});

export { styles };